---
interface Props {
  as?: string;
  class?: string;
}
const { as: Tag = 'div', class: className = '' } = Astro.props;
---
<Tag class:list={["mx-auto w-full max-w-[1120px] px-6 md:px-10", className]}>
  <slot />
</Tag>
